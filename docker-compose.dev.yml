version: "3.8"

services:
  demo-go-api:
    container_name: demo-go-api-dev
    image: demo-go-api-dev
    build:
      context: .
      dockerfile: compose/dev/Dockerfile
    volumes:
      - .:/app
    environment:
      - PORT
    ports:
      - ${PORT}:${PORT}